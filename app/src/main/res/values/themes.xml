<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Yakuba" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="background">@color/white</item>
    </style>
    <style name="Theme.Yakuba" parent="Base.Theme.Yakuba">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen</item>
        <item name = "windowSplashScreenAnimationDuration">1500</item>
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="pinViewStyle">@style/PinWidget.PinView</item>
    </style>

    <style name="AppBarWhite" parent="Widget.MaterialComponents.Toolbar">
        <item name="background">@color/white</item>
        <item name="titleTextColor">@color/black</item>
        <item name="subtitleTextColor">@color/black</item>
    </style>

</resources>